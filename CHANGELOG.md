# Changelog

## Unreleased

### Added

- 五份 PDF 现在同步生成逐页 PNG 图片版；每页以 144 DPI 直接栅格化，不裁剪、不重排、不拼接。
- 新增 ReportLab PDF renderer；统一榜单命令现在同步生成五个 XLSX 和五个同名 A2 PDF，每份 PDF 固定为进攻方、防守方和补丁说明三张大页。
- README 新增五张进攻/防守交错的报告预览，并链接 `docs/` 中可直接打开的 PDF 示例。

- 新增 `~archived.md`，记录本地大体积归档的用途、结构、审计容量、禁止推送原因及恢复方法，供远程脱敏导出替代 `~archived/`。
- 新增 `r6_report.collector` 原子采集流程：在临时目录验证 Wiki 表、补丁、干员 Badge、白色图标和次要装备图标后再整体启用。
- 新增结构化来源契约，记录 Athieno 视频覆盖补丁、Wiki 当前赛季/补丁、带时区抓取时间和严格补丁日期区间。
- 每张工作表底部新增三行可点击来源说明；补丁工作表改为按补丁发布日期动态分组。
- 根目录新增唯一入口 `run_r6_report.bat`，依次运行采集、基础统计表和五榜生成。
- 三种工作簿均增加独立“补丁说明”工作表，汇总 Y11S2.1、Y11S2.2 中影响强度的增强、削弱、混合调整及相关修复。
- 补丁说明加入灰机 Wiki 与 Ubisoft 官方 HTTPS 来源链接，以绿、红、黄分别标记增强、削弱和混合变化，并显示对应 Athieno 视频评分供对照。
- 所有进攻/防守数据表和简图在最后一行增加统一数据状态说明，明确除 Athieno Y11S2 视频评分外，其他信息均按生成时的灰机 Wiki 最新数据更新。
- 增加“主狙”“副手霰弹”“次要装备”三列。
- 增加项目使用说明、数据分类规则和测试命令。
- 增加 `r6_tier_chart.py`，从九列统计工作簿生成进攻/防守两张传统横向 Tier List 简图。
- Tier 简图按评分阶层从上到下排列，每行最多五名干员，并显示 Badge、速度、主副手自动枪械射速、`主狙`/`副喷`状态。
- 增加灰机 Wiki 官方次要装备图标解析与本地缓存，图标角标显示数量。
- 增加 `run_r6_tier_chart.bat` 一键生成简图工作簿。
- 增加 `r6_rpm_chart.py` 和 `run_r6_rpm_chart.bat`，按主副手自动枪械最高 RPM 生成进攻/防守射速榜。
- 射速榜使用传统横向 Tier List 布局，左侧按最高自动枪械射速显示 `Ⅰ／Ⅱ／Ⅲ／Ⅳ` 固定阶层，每档每五名干员换行。
- 射速榜工作簿增加“进攻方主手射速榜”和“防守方主手射速榜”，只按每名干员主手自动枪械的最高 RPM 分档和排序。
- 增加 `r6_tiers.py`，统一可见阶层、颜色和原始 `boof` 标签的展示规范化。
- 增加 `r6_leaderboards.py` 和 `run_r6_leaderboards.bat`，一次在 `output/` 生成视频 Tier、主武器射速、速度、稀有枪械和次要装备五个独立榜单工作簿。
- 五榜使用统一四行卡片：名字/视频 Tier/速度，副喷/主狙，副手 RPM/主手 RPM，以及全部次要装备图标。
- 稀有枪械榜和次要装备榜支持同一干员按多个匹配等级重复出现。

### Changed

- PDF 次要装备行移除装备名称；PDF 与 XLSX 统一使用每行四个固定槽位，图标在各自槽位内居中。
- PDF 与 XLSX 榜单省略次要装备数量标记；装备图标统一为与干员卡片正文行等高，并继续保持原始长宽比。
- PDF 与 XLSX 榜单改为共享字体、字号、文字颜色、卡片底色、边框色和补丁方向颜色；README 十张工作簿预览同步更新。
- `副喷`、`主狙` 的“有”状态统一显示为勾号；所有语义明确的 `-` 缺失字段统一使用灰色底。
- README 五个榜单均改为左侧进攻方、右侧防守方的双栏预览，并补齐十张 PDF 页面预览图。
- 将 `$build-r6-operator-report` 迁移到根目录 `skills/` 并收缩为单一职责：只从 Athieno 最新完整视频人工核对并更新 Tier 数据；README 保留其为本地部署步骤。
- PDF 三个章节页改为保持 420 mm 宽并按实际内容自动收缩高度，消除 README 预览的大块页底空白；生成文件清单移至 README 末尾。
- README 精简为“功能”和“本地部署”两部分，保留报告预览并移除内部数据规则、目录和远程发布说明。
- PDF 各等级标题由短色块改为贯穿 A2 可用内容区的整行色带；进攻方、防守方和补丁说明分别固定在单独大页，README 五张预览同步更新。
- 将本地默认分支由 `master` 重命名为 `main`，与脱敏导出和远程发布流程保持一致。
- 在 README 中补全源仓库、`data/`、`~archived/`、`secrets/`、`~ref/` 与 `output/github-export/` 的发布边界和仅 fast-forward 的 `main` 发布流程。
- 将 `docs/superpowers/plans/` 中 7 份已完成计划移入 `~archived/superpowers-plans/`，使计划目录只保留当前执行内容。
- 按全局 `AGENTS.md` 重新审计仓库：确认仅忽略根目录 `output/` 与 `~temp/`，并将根目录临时工具残留及会继续生效的旧归档规则文件移入带日期的非活动归档。
- 所有榜单卡片在视频 Tier 后显示评分视频至当前数据快照之间的补丁方向：`+` 为增强、`-` 为削弱、`~` 为混合。
- 补丁说明中的增强、削弱和混合改用高对比绿、红、黄底色与白色粗体文字。
- 次要装备图标每行最多四个；Striker、Sentry 等拥有七种装备的干员自动换为两行。
- 取消卡片内第一档特性文字和次要装备图标的红色强调；左侧榜单分档颜色保持不变。
- 五榜文件名改为 `视频评分榜.xlsx`、`主武器射速榜.xlsx`、`速度榜.xlsx`、`稀有枪械榜.xlsx`、`次要装备榜.xlsx`。
- 生成阶段改为只读取 `data/` 中已验证快照，不再在工作簿生成过程中联网补齐数据或图标。
- 稀有枪械榜左侧分档简化为 `副喷 / 主狙 / 副自 / 都无`。
- 卡片之间增加独立留白列，缩短 Badge 与本卡文字的距离，并将装备图标锚定在本卡信息区域。
- 项目本地 Skill 改为先人工核对 Athieno 最新完整 Tier List，再运行统一 BAT。
- 旧 BAT、根目录旧工作簿、旧静态资产和 Skill 内重复脚本已归档到 `~archived/2026-07-25-project-rebuild/`。
- 整理项目根目录，将旧 RPM 单榜生成器、旧单榜 BAT、对应旧测试、Skill 暂存内容和 QA 临时产物移入 `~archived/2026-07-25-root-cleanup/`。
- `r6_tier_chart.py` 保留为五榜生成器依赖的共享解析与图标模块，不再提供单独 BAT 入口。
- 将榜单级内排序统一为视频 Tier、主武器射速档、速度、稀有枪械、次要装备的全局顺序，并跳过当前榜单维度。
- 将所有可见“主手半自”统一改名为“主狙”，分类规则保持不变。
- 将 `P10 RONI转换套件衍生型` 统一显示为 `P10 RONI`。
- 生成的五榜文件统一写入被 Git 忽略的 `output/`。
- 新增 `r6_patch_notes.py`，由三个生成器共享补丁元数据、来源校验、补丁工作表渲染和底部状态行。
- 补丁变化只作为视频版本后的信息说明，不修改 `data/athieno_y11s2.json` 或任何可见评分。
- Tier 与射速简图读取结构化 Excel Table 的实际数据范围，忽略 Table 外的底部数据状态说明，同时继续兼容没有 Table 的九列表输入。
- 加宽补丁来源标签列并启用自动换行，避免 Excel 打印视图截断来源名称。
- 射速榜固定使用 `1000 / 850 / 750` 三个 RPM 分界，当前四档分布为 `19 / 19 / 18 / 21`。
- 主手射速榜由全部 77 名干员合并四等分后固定使用 `860 / 780 / 700` 三个 RPM 分界，当前四档分布为 `20 / 20 / 18 / 19`；同射速不拆档。
- 将 RPM 罗马数字移至左侧合并阶层栏；卡片名字右侧的小彩色徽标显示 Athieno 视频 Tier `S/A/B/C/D/F`，不再把两种 Tier 混用。
- “补丁说明”工作表取消冻结窗格，标题、来源、筛选和打印设置保持不变。
- 将自动枪械拆分为“主手自动枪械”和“副手自动枪械”，两列分别按射速降序显示。
- 使用 `WeaponData.equipment` 判定主副手，使用 `WeaponData.type`、`projectile` 判定副手霰弹。
- 从 `Operator.props` 读取并换行显示次要装备。
- 清理次要装备字段中的 MediaWiki 链接标记和残留方括号。
- 工作簿扩展为九列，并按内容行数动态调整行高。
- 同步更新项目本地 `build-r6-operator-report` Skill。
- 原始 `boof=0` 继续保留用于来源追溯，但所有工作簿均将其显示并着色为当前最低字母档 F。
- Tier 简图移除独立 BOOF 行，原 Boof 干员并入 F 行。
- 将第 6 列统一为“主狙”，明确只统计主手中 `firerate == 0` 且 `projectile == 1` 的单弹丸非自动枪械，包含主手独头弹霰弹并排除多弹丸主手霰弹。
- 将“副手霰弹”严格改为 `equipment == 2` 且 `projectile > 1`，不再依赖武器类型文本。

### Fixed

- 修复 XLSX 一行四件次要装备时图标挤在卡片左侧的问题，现分别使用四个等距信息列。
- 修复 Striker、Sentry 的 7 个次要装备在 XLSX 中显示不全的问题；现固定按 4+3 两行排列，并用共享尺寸保证全部图标位于装备行可视范围内。
- 修复 PDF 次要装备图标被强制拉伸为固定宽高的问题，现按原始长宽比在固定边界框内居中缩放。
- 修复 PDF 补丁表方向着色后正文被误设为白色而不可见的问题。
- 将根目录 BAT 固定为 UTF-8、CRLF，并强制 Python 使用 UTF-8 输出，避免双击后命令被拆分、窗口闪退或中文乱码。
- curl 返回本地化错误文本时使用容错解码，不再因非 UTF-8 字节触发后台解码异常。
