# Changelog

## 2026-08-04

### Added

- 为 XLSX 与 PDF 卡片加入全部匹配枪械图标，采集流程新增 75 个已验证的离线枪械图标。
- 五份 PDF 同步生成 144 DPI 逐页 PNG，并将完整报表与图片版交付到 `~outputs/`。
- 新增项目专用 `AGENTS.md`、开发上下文、全局目录契约测试和标准计划归档目录。

### Changed

- 干员卡片在左侧徽章框的完整尺寸图标下方恢复单行干员名字，卡片总高度保持不变。
- 干员卡片首行左半区再等分为彩色视频 Tier 与速度；副喷下方新增仅覆盖右半区的强调分界线。
- 加深并强化干员卡片的外框、左右中央线、行线及七个次要装备槽边界，使 XLSX、PDF 与 PNG 的内容分区更清晰。
- 干员卡片前四行统一为对齐的左右半区；顶部左侧将视频 Tier 与速度合并为一格，右侧显示主狙，底部仍保留七个等宽次要装备槽。
- 干员卡片移除名字并改为五行七槽网格：顶部依次显示速度、视频 Tier 和主狙；中间三行左侧显示主要武器，右侧显示副喷及两把副武器；底部单行显示七个带分界线的次要装备槽。
- 干员卡片下半区改为左右分栏：次要装备压缩到左半边，主要武器在右半边按“一行一个射速与枪械图标”展示，最多三行；XLSX、PDF 与 PNG 布局保持一致。
- 各榜单等级内优先按自身原始指标降序排列，再使用其他维度和来源顺序打破平局。
- 原始输入、临时工作簿、生成结果和历史归档分别迁移到 `inputs/`、`~temp/`、`~outputs/`、`~archive/`。
- CLI 输入参数统一为 `--inputs-dir`；统一 BAT 使用新目录并继续保持 UTF-8、CRLF 与失败即停。
- 已完成计划迁入 `docs/finished_plans/`，历史技能规格封存到 `~archive/superpowers-specs/`。
- Python 缓存停止由 Git 跟踪；补齐 `.venv/`、`.worktrees/`、`~temp/` 和 `~outputs/` 的精确 ignore 规则。

### Fixed

- 将 Pillow 依赖约束更新到 12.x，使新建环境与当前图像测试 API 一致。

## 2026-07-28

### Added

- PDF 新增逐页 PNG 图片版，README 增加十张进攻方/防守方双栏预览。

### Changed

- PDF 与 XLSX 的次要装备统一使用固定槽位和纯图标展示。
- 纯次要装备平衡改动不再生成干员卡片补丁标记，但仍保留在补丁说明中。
- 冲击 EMP 图标改用官方线稿来源。

### Fixed

- 修复 XLSX 装备图标挤在卡片左侧、PDF 图标位置不一致和图标过小的问题。
- 修复七件次要装备无法完整显示以及 PDF 图标长宽比失真的问题。

## 2026-07-27

### Added

- 新增 XLSX/PDF 共享报表主题，统一字体、颜色、边框和补丁方向样式。

### Changed

- 次要装备图标与正文行对齐；PDF 装备行移除名称和数量文字。
- `副喷 / 主狙`、射速和缺失字段在五份榜单中使用一致的显示规则。

## 2026-07-26

### Added

- 新增 ReportLab PDF renderer，每份榜单固定生成进攻方、防守方和补丁说明三页。
- 新增原子采集流程、结构化来源契约、补丁方向标记和固定报表示例。
- 将 Athieno Tier 核对收敛为单一职责的项目 Skill。

### Changed

- PDF 页面保持 420 mm 宽并按内容自动调整高度。
- 报表生成阶段只读取已验证快照，不再联网补齐数据或图标。
- README 精简为功能、部署与使用说明。

### Fixed

- 修复 BAT 中文乱码、异常退出码丢失和本地化 curl 错误文本解码问题。
- 修复 PDF 补丁正文颜色和页面底部大块空白。

## 2026-07-25

### Added

- 建立 `r6_report` Python 包、统一三阶段 BAT 流水线和 `unittest` 测试套件。
- 支持视频评分、主武器射速、速度、稀有枪械、次要装备五种中文榜单。
- 加入 Wiki 干员、武器、图标与补丁采集，生成结构化基础统计工作簿。
- 榜单卡片显示干员图标、Tier、速度、主副手射速、主狙、副喷和次要装备。

### Changed

- 旧脚本、重复资产、旧工作簿和一次性 QA 材料移入本地归档。
- 可见最低档统一显示为 F；主手单弹丸非自动武器统一称为“主狙”。
- 主副手自动枪械分列并按射速排序，稀有枪械与次要装备支持多等级重复出现。

### Fixed

- 严格校验评分覆盖、Wiki 结构、补丁日期区间和图标完整性；采集失败时保留上一份有效快照。
